[build]
  command = "rm -rf dist && unzip -o 'atomian_deploy[1].zip' -d dist"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
