# Classic version (original React SPA) served from dist/
/classic/assets/*  /dist/assets/:splat  200
/classic/images/*  /dist/images/:splat  200
/classic           /dist/index.html     200
/classic/          /dist/index.html     200
/classic/*         /dist/index.html     200

# Absolute asset paths baked into the old React bundle
/assets/*          /dist/assets/:splat  200
/images/*          /dist/images/:splat  200

# SPA fallback for the new landing page
/*                 /index.html          200
